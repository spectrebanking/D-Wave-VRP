# dwave-qbsolv (used for CPU solving) is unmaintained and requires dimod<0.11,
# so these are pinned rather than pulling in current dwave-ocean-sdk.
numpy<2.0.0
networkx
dimod==0.10.17
dwave-qbsolv
dwave-hybrid==0.6.5
