---
description: Plain-English rollup of blockers, the critical path, and the opportunity pipeline
---

Run:

```
uv run python scripts/status.py
```

Renders the blocker critical path (SAM activation = node zero), the single next
action + owner + follow-up cadence, and a count of opportunities per pipeline stage.
